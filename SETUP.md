# ARIDO - Build Configuration\n\n## Firebase Firestore Rules (Test Mode)\n\n```javascript\nrules_version = '2';\nservice cloud.firestore {\n  match /databases/{database}/documents {\n    // Allow all reads and writes for now (Test Mode)\n    match /{document=**} {\n      allow read, write: if true;\n    }\n    \n    // Specific rules for messages\n    match /messages/{messageId} {\n      allow read: if true;\n      allow create: if request.auth.uid != null;\n      allow update, delete: if request.auth.uid == resource.data.userId;\n    }\n    \n    // Specific rules for users\n    match /users/{userId} {\n      allow read: if true;\n      allow create, update: if request.auth.uid == userId;\n      allow delete: if request.auth.uid == userId;\n    }\n  }\n}\n```\n\n## Android Manifest Configuration\n\nAdd these permissions to `android/app/src/main/AndroidManifest.xml`:\n\n```xml\n<!-- Internet Permission -->\n<uses-permission android:name=\"android.permission.INTERNET\" />\n\n<!-- Camera Permission -->\n<uses-permission android:name=\"android.permission.CAMERA\" />\n\n<!-- Microphone Permission -->\n<uses-permission android:name=\"android.permission.RECORD_AUDIO\" />\n\n<!-- Network State Permission -->\n<uses-permission android:name=\"android.permission.ACCESS_NETWORK_STATE\" />\n\n<!-- Change Network State Permission -->\n<uses-permission android:name=\"android.permission.CHANGE_NETWORK_STATE\" />\n\n<!-- Modify Audio Settings Permission -->\n<uses-permission android:name=\"android.permission.MODIFY_AUDIO_SETTINGS\" />\n```\n\n## Firebase Console Setup\n\n### Firestore Database\n1. Create Database in Production Mode\n2. Rules:\n   - Test Mode (30 days): Allow all\n   - Production Mode: Implement proper security rules\n\n### Authentication\n1. Enable Anonymous Sign-in\n2. Optional: Enable Email/Password or Google Sign-in\n\n### Cloud Functions\n1. Deploy from `functions/` directory:\n```bash\ncd functions\nfirebase deploy --only functions\n```\n\n## ZegoCloud Setup\n\n1. Create Account at https://console.zegocloud.com\n2. Create New Project: \"Arido\"\n3. Get AppID and AppSign\n4. Update in `lib/config/constants.dart`\n\n## Codemagic Build Configuration\n\n```yaml\napp:\n  build:\n    android:\n      release: true\n      args: --release\n\nworkflows:\n  default-workflow:\n    name: ARIDO Build\n    instance_type: mac_mini\n    max_build_duration: 60\n```\n\n## Development Commands\n\n```bash\n# Get dependencies\nflutter pub get\n\n# Run app\nflutter run\n\n# Build APK\nflutter build apk --release\n\n# Build AAB (for Play Store)\nflutter build appbundle --release\n\n# Check code\nflutter analyze\n\n# Format code\ndart format lib/\n```\n\n## Testing\n\n```bash\n# Run tests\nflutter test\n\n# Run tests with coverage\nflutter test --coverage\n```\n\n## Deployment Checklist\n\n- [ ] Firebase Project Created\n- [ ] Firestore Database Enabled\n- [ ] Authentication Configured\n- [ ] Cloud Functions Deployed\n- [ ] ZegoCloud Project Created\n- [ ] google-services.json in place\n- [ ] Constants updated\n- [ ] AndroidManifest.xml permissions added\n- [ ] APK built successfully\n- [ ] Tested on physical device\n- [ ] Ready for Codemagic deployment\n\n## Troubleshooting\n\n### Firebase Connection Issues\n- Verify google-services.json is in `android/app/`\n- Check Firebase Project ID matches\n- Ensure internet permission is granted\n\n### ZegoCloud Call Issues\n- Verify AppID and AppSign are correct\n- Check network permissions\n- Ensure camera and microphone permissions are granted\n\n### Build Errors\n- Run `flutter clean`\n- Delete `.gradle` folder\n- Run `flutter pub get` again\n- Rebuild the project\n